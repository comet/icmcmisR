<table class="data">
  <tr>
    <th>Subject</th>
    <th>Physician</th>
    <th>Date</th>
  </tr>
<% @appointments.each do |appointment| %>
  <tr>
    <td><%= link_to appointment.subject,appointment %></td>
    <td><%= appointment.physician_id %></td>
    <td><%= appointment.appointment_date %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Appointment', new_patient_appointment_path,:class=>"button" %>
